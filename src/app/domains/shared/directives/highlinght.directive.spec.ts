import { HighlinghtDirective } from './highlinght.directive';

describe('HighlinghtDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlinghtDirective();
    expect(directive).toBeTruthy();
  });
});
